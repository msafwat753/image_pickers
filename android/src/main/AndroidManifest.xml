<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.leeson.image_pickers">

    <!-- Required permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Removed: <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" /> -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Limit queries to only image capture -->
    <queries package="${applicationId}">
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE"/>
        </intent>
        <!-- Removed video capture intent -->
    </queries>

    <application
        android:networkSecurityConfig="@xml/net_sec"
        android:requestLegacyExternalStorage="true">
        
        <!-- For compatibility with Android 9.0 -->
        <uses-library android:name="org.apache.http.legacy" android:required="false" />
        
        <activity android:name=".activitys.PermissionActivity" android:theme="@style/TransTheme"/>
        <activity android:name=".activitys.SelectPicsActivity" android:theme="@style/TransTheme"/>
        <activity 
            android:name=".activitys.PhotosActivity" 
            android:configChanges="orientation|screenSize" 
            android:theme="@style/AppTheme"/>
        <!-- Removed: <activity android:name=".activitys.VideoActivity" ... /> -->
    </application>
</manifest>
